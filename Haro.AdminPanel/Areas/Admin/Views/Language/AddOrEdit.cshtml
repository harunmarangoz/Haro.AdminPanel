@model AddLanguageModel

@{
    var isNew = Model.Id == 0;
}

@section topButton{
    <a asp-action="List" class="btn btn-outline-primary">Listeye Dön</a>
}

<ajax-form asp-action="@(isNew ? "Add" : "Edit")">
    <form-input for="Id" type="Hidden" />
    <form-input for="DisplayValue" type="Text" />
    <form-input for="Code" type="Text" />
    <form-input for="Image" type="Text"  />
</ajax-form>

@section js{
    <script>
        $('#Code').keyup(function () { $('#Image').val('https://flagcdn.com/160x120/' + $('#Code').val() + '.png'); });
    </script>
}